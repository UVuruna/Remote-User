# CLAUDE.md — Vibe Coder

Project-specific guidance. Inherits the [root constitution](../../CLAUDE.md) —
read it first, then use its [Router](../../CLAUDE.md#router) to load only the
rulebook(s) your job needs (`rules/CODE.md` for implementation,
`rules/DOCS.md` for documentation, `rules/SHIP.md` for build/release, etc.).
This file only ADDS or TIGHTENS rules for this project — it never restates or
loosens the root's. The four base guard tests plus the two GUI guards (THE
SPACE & LEGIBILITY LAW — [GUI Rules](../../rules/GUI.md)) + Claude Code hooks
([Code Rules](../../rules/CODE.md) → Enforcement) are installed in `tests/`
(`test_structure_law.py`, `test_config_sections.py`, `test_docs_coverage.py`,
`test_doc_links.py`, `test_layout_law.py`, `test_layout_audit.py`, plus
`test_layout_audit_qt.py` auditing the desktop Qt windows) and wired via
`.claude/settings.json` (`run_guards.py --fast` on every Edit/Write,
`run_guards.py` full on Stop). GUI work here is also governed by the Zubi v2
algorithmic teeth and Grader v2 — **installed** (`test_layout_audit_qt.py` is
the project's thin entry point onto `rules/templates/layout_checks_qt.py`,
[GUI Rules](../../rules/GUI.md) → Zubi v2). `tests/` also carries many
project-specific guards (see `tests/___tests.md`) beyond the standard set.

---

## What This Project Is

Remote control of the Windows PC from an Android tablet/phone over LAN. The PC runs a Python server (screen capture + input injection); the tablet runs **no native app** — it opens a web page served by the PC. Full architecture and design decisions: [Vibe Coder](README.md). Phase status: [Roadmap](ROADMAP.md).

## Tech Stack (decided — do not relitigate without owner approval)

**The authoritative record of the product direction and build plan is [ROADMAP.md](ROADMAP.md#direction) (Product Direction, decided 2026-07-22). Read it before proposing architecture changes.** Summary:

- **Server:** Python 3.13, FastAPI + uvicorn, `dxcam` (DXGI capture), `ctypes` → Win32 `SendInput` (injection), `qrcode` (pairing). Two entry points around one `server_core`: `gui_main.py` (PySide6 window + tray — what the packaged EXE runs) and `main.py` (headless CLI). `bootstrap.init_process()` (DPI → logging → user settings) MUST run before any screen-touching import.
- **Streaming:** **H.264** via ffmpeg, hardware-encoded with auto-detection (NVENC → QuickSync → AMF) and a **libx264 software fallback** so it runs on any PC; output is fragmented MP4 decoded by the client via MSE, at **native monitor resolution** by default (`h264_max_width`). JPEG-per-frame is the fallback when no ffmpeg/encoder exists. (Measured ~3.6 Mbps static native 4K vs ~37 Mbps JPEG at quarter resolution.) Bundled ffmpeg is **pinned to 7.1.1** — newer builds require NVENC API 13.1 (driver ≥ 610) and silently drop to software encoding on older drivers.
- **Product = two installable apps:** a Windows desktop app (server + GUI + tray, `.exe`) and an Android **hybrid** app (native Kotlin shell in `android/` + WebView reusing the web client, shipped as an APK; the shell adds only what a browser cannot — pairing, real-app link routing, file chooser, `Android.rescan()` / `Android.setTailscaleUrl()` bridge — ALL guidance lives once, in the page). The shell stores **two addresses** (LAN from pairing + Tailscale learned from every `config` via the bridge) and probes `/ping` on both at start, loading whichever answers — the app connects at home AND on mobile data with no user choice (a single stored URL was the live failure: LAN address on mobile data = minutes of `ERR_CONNECTION_TIMED_OUT`). The probe counts ONLY the exact 204 as alive (captive portals on foreign Wi-Fi answer anything with a 2xx/redirect login page — live failure 2026-07-27; the 204 contract is pinned in the build gate), the error card keeps re-probing by itself (4 s timer + instant re-probe on every default-network change) because a one-shot "Try again" always fired mid-flap on flaky foreign Wi-Fi, that card names the CAUSE instead of one generic message (owner 2026-08-04 — no network / PC not on Tailscale / Tailscale missing / Tailscale off / PC down, read from network capabilities + package visibility, with the primary button being the fix: Install Tailscale, Turn Tailscale on — the shell opens the app since Android cannot flip another app's VPN switch — or Try again), and a session running over an unfamiliar Wi-Fi (LAN dead, tunnel alive, Wi-Fi transport) gets a one-per-stay security toast. **NO browser EVER shows the client — on ANY device** (owner rule 2026-07-22, shouted; hardened 2026-08-02 after the live tablet failure: Chrome on Android tablets sends a desktop-Linux User-Agent with no `Android` token, so "detect Android" routing served the tablet the live client): the server serves the client ONLY to the APK's WebView (it appends a `VibeCoderApp` User-Agent marker) and routes every other User-Agent to `client/install.html` — a full-screen funnel whose FIRST, primary button is **Open the app** (`intent://pair?url=…` hands the tokened URL to the app; it pairs itself — nothing typed, nothing scanned; a camera-scanned QR always lands in the browser first, an Android platform limit for plain-http links, so every post-install scan must be exactly one tap), with the one-time Install (`/app.apk`) below it. After install the QR is not part of daily use — the user just opens the app; re-pairing happens IN the app. Desktop browsers get the funnel too (dev exception: only a checkout with no APK built serves the client — an official install always bundles the APK). The desktop installer bundles the APK.
- **Connection:** a mesh VPN installed on both devices (Tailscale recommended), one-time, wizard-guided; works anywhere incl. mobile data via the mesh's free relay. Server already binds all interfaces + pairing detects the Tailscale address, so this is code-ready.
- **Login = the Tailscale login** (owner decision, option a): the mesh identity IS the account; one login per device, no custom account backend.
- **The app installs/drives all dependencies — the user never side-installs anything** (hard owner requirement). Wizard in the `.exe` AND the APK: ffmpeg bundled; Tailscale chain-installed on desktop / store-deep-linked-and-detected on Android; user only follows in-app prompts. Never tell the *end user* to download/copy/configure anything (telling the *owner* to install a dep for dev-time testing is fine, but say so explicitly).
- **Client UI:** vanilla HTML/CSS/JS in the WebView, Pointer Events, canvas rendering — no framework, no build step.
- **Hard constraints (owner):** no payment for any required part; third-party services/installs OK if best; not over-indexing on privacy/security. Don't fight mature remote-desktop tools on latency — the unique value is the **app-aware companion layer** (Windows UI Automation state reading, notifications, per-app controls).
- **Open decisions** (see ROADMAP): mesh provider (Tailscale vs ZeroTier), distribution (APK vs later Play Store), build order.

## Session Workflow (owner decree 2026-07-22)

**ALWAYS finish a session that touches server/client/android code by running the FULL desktop build AND publishing a GIT RELEASE** (`setup/build.py` — it bundles the client and the freshly built APK into the installer). The owner tests and updates ONLY through the official GitHub release (see [Ship Rules](../../rules/SHIP.md) → Self-Update), never the dev server from VSCode and never the local `dist/` — shipping changes without a **released** build means the owner installs stale software and the fixes "don't work". Build the APK first (`setup/build_apk.py`) when android/ or client/ changed. **Do ALL of this — build and GIT RELEASE — automatically, without being asked** (THE RELEASE LAW, root [CLAUDE.md](../../CLAUDE.md) → The Laws / [Ship Rules](../../rules/SHIP.md)).

## Architecture Constraints

1. **One monitor per view.** The client always displays and controls exactly one monitor; a switch button changes which. Client coordinates are ALWAYS normalized 0–1 within the displayed monitor; the server maps them to that monitor's rect. Never send virtual-desktop-wide coordinates.
2. **The server process must declare DPI awareness** (`DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2`) before any capture or injection — otherwise Windows silently rescales injected coordinates.
3. **No input before auth.** The WebSocket handler rejects every message until a valid token arrives. This is a hard security rule (Remote Mouse CVE class), not a nice-to-have.
4. **Unicode text goes through `KEYEVENTF_UNICODE`** (VK_PACKET), never through virtual-key mapping. Special keys (Enter, Backspace, arrows, Tab, Esc) go through VK codes. Two distinct protocol messages: `key_text` vs `key_special`.
5. **Client keyboard capture:** an invisible but REAL-SIZE focusable `<textarea>` — full-width, ~42px tall, at the top, rendered fully transparent (transparent text/caret/background) with `pointer-events:none`, font ≥16px. NOT `display:none`, NOT `opacity:0`, and NOT 1px×1px: a 1px field made Android IMEs silently refuse to raise the keyboard / attach voice input (found live 2026-07-22 — typing and dictation both dead). A textarea so the phone IME shows ↵/new-row instead of a Send/Go key; `autocapitalize/autocorrect/autocomplete/spellcheck` disabled; printable characters via input-event diffing (the diff replays surviving-suffix edits too), structural keys via `keydown`. Never trust `keydown.key` for printable characters (Android IME).
6. **The finger steers, buttons act — and NOTHING is a tap** (owner decision 2026-07-22, hardened 2026-07-26): the one-finger gesture ONLY moves the PC cursor — it never clicks. Left AND right clicks are explicit press buttons (`click {button}` — down+up at the current cursor position; twice fast = double click); the old right-click-on-tap mode is GONE — with the offset pointer a tap would land away from the fingertip, so no canvas tap may ever act. Toggle modes (drag/scroll) stay; two fingers = local pinch zoom/pan, never clicks. No timed gestures, ever. **The pointer sits EXACTLY under the finger** (owner decision 2026-08-02, reversing the 2026-07-26 offset system after living with it on-device): no handedness diagonal, no calibration, no reserved edge margins — the image aspect-fits the FULL canvas and a focused layout touches all four screen edges. `config.hand` is GONE (owner decree 2026-08-07: *"briši sve vezano za to levu desnu ruku ... i sve ono što smo računali offset ... ne držimo legacy stvari već ih uklanjamo"*) — the server no longer sends it, there is nothing left to ignore, and the `calibrate` action, its icon and every doc page still describing the handedness diagonal, the calibration samples and the reserved edge margins went with it the same day. Those pages were the real danger: the code had been dead since 2026-08-02 while the documentation still explained it as live, naming functions that no longer exist. The design going forward is gamepad-first, not finger-handedness.
7. **Ghost-pointer self-healing** (live bug 2026-07-22): Android WebView occasionally loses a `pointerup`/`pointercancel`; a stale entry in the pointer map made EVERY later tap register as a pinch until page refresh ("input randomly stops working"). On every `pointerdown` with `isPrimary`, the client wipes pointer/pinch/primary state — a new primary pointer is the browser's guarantee no other finger is really down. Do not remove this reset.
8. **Session lives only while the owner is looking** (owner security decision): the client closes the WebSocket when the page is hidden (tab backgrounded / screen locked) and reconnects **immediately** on return (`visibilitychange` → connect, not just the 2 s retry interval — the wait swallowed the first taps after every app switch). A `send()` on a dead socket must stay VISIBLE (status pill "Reconnecting…") — silently dropping it read as "buttons randomly stopped working" over a frozen frame. **UIPI is the proven total-input killer and is now DEFENDED twice** (live failure 2026-07-29, the recurring "every release breaks the mouse" ghost): Windows silently discards ALL `SendInput` from a non-elevated process — the call still returns success — whenever an ELEVATED window has focus (the owner's admin VSCode after every dev session; stream fine, zero errors, dead mouse; confirmed by controlled A/B on the installed app — medium = zero movement, elevated = pixel-exact). Defense: (1) the packaged app runs **elevated always** (`--uac-admin`; autostart via Task Scheduler `/RL HIGHEST` because HKCU Run silently skips elevated apps); (2) the injector **verifies its own moves** (`InjectionMonitor`: commanded jump ≥ threshold must land; a miss streak → server ERROR log + client toast) — if anything ever eats input again (lock screen, future policies), the phone SAYS so instead of gliding a local cursor over a dead session. The monitor's logic is pinned in the build gate; never remove either defense.
9. **Control buttons fire on pointerup PLUS a pointercancel rescue, and the input pipeline is gated** (live failure 2026-07-26 — every button dead on the real device, only the keyboard toggle alive): Android ends touches that start near screen edges / system-gesture zones with a `pointercancel`, so an up-only tap handler simply never runs — and with `targetSdk 35` the WebView is drawn edge-to-edge, so the nav bar also sat ON TOP of the bottom controls (the shell now hides the system bars — immersive, swipe reveals them transiently). `keepFocus` therefore fires on pointerup (touch grants transient user activation at UP — the file picker and IME focus need it; plain down-firing broke them) AND on a pointercancel whose travel stayed under `CANCEL_TAP_SLOP` — a stolen tap still acts, while a system home/back swipe crossing a button (real travel, then cancel) never does. Never simplify this back to bare `click`/pointerup, and never remove `tests/test_input_pipeline.py` from `build.py` (the fail-closed INPUT GATE that proves touch → protocol → injection end-to-end before anything ships; its layout comes from `tests/fixtures/actions.json`, pinned so owner edits to the real actions.json can't block builds).

10. **Nothing we raise may outlive us in the always-on-top band** (owner decree 2026-08-05, after his own Chrome and VSCode were left nailed above everything twice). Every window the phone raises is written into a LEDGER (`window_manager.mark_topmost`), and there are exactly two ways out, one per way the process can end: (a) we get to run code — tray Quit, server stop, Apply & restart, Ctrl+C, a console close, an unhandled crash, logoff — and `release_all()` walks the ledger, wired into `ServerController.release_windows()`, Qt `aboutToQuit`, `atexit` and a `SetConsoleCtrlHandler`, all idempotent; (b) we do NOT — Task Manager, the installer's `taskkill`, a power cut — so the ledger is mirrored to `%LOCALAPPDATA%/VibeCoder/topmost.json` on every change and `repair_stranded()` fixes it at the next start, touching a window ONLY while it still runs the executable it ran when we raised it (a recycled handle must never cost a stranger's window its z-order). `raise_window(hwnd, topmost=False)` exists for the transient raises that are NOT layout membership (tab extraction, `next_input`) — those windows belong to no layout, so a topmost raise there stranded them for the rest of the Windows session. `prune()` drops only CLOSED windows, never merely cloaked ones: Windows cloaks every window on another VIRTUAL DESKTOP, so pruning on visibility deleted the owner's layout the moment he pressed Win+Ctrl+Right and abandoned its members up there.

11. **What the phone types lands where the owner is LOOKING** (owner report 2026-08-06, three times in one evening — and the fourth report was the bug itself: a sentence he dictated for another project arrived in this project's session). `SendInput` has no target: every dictated character goes to whatever window Windows calls the FOREGROUND at that instant, so anything on the PC that takes focus mid-sentence — an app starting, a dialog, another agent's editor window — takes the rest of the sentence with it, silently, while the stream still shows the PC. `server/focus_guard.py` decides the target BEFORE every message that types (`key_text`, `key_special`, `chord`, `paste_text`, `screenshot`): **in a layout the fence is the layout** — a foreground outside its members is refused, focus goes back to the member the phone was last typing into, and only then do the keys go out; **at the full desktop** the target is the window the typing burst started in, re-armed only by what the owner does on purpose (an injected click, `next_input`, a layout/monitor switch — a thief sends no message and arms nothing); **a dialog of the target is the target** (owner chain, never process identity — every VSCode window shares one process and one of them is exactly the thief); and **the thief is NAMED in the log** every time, because the app being the last to know is what cost three reports. The other half of the same bug lives in `Layout.last_member`: `focus()` raises the keyboard member LAST, since one excursion (picker, permission dialog) closes the socket, the page re-focuses the layout, and raising members in list order handed the keyboard to whichever window sat last in the grid. Gate: `tests/test_focus_guard.py`, fail-closed in `build.py` (0e/6) — never remove either half. **And a focused layout is DEFENDED, not merely checked** (owner decree the same evening, shouting — "kada uhvatimo fokus lejauta ne može nikakav program da izbaci fokus"): `focus_guard.watch` is one task per connection that polls the foreground every 0.25 s and puts it straight back inside the layout, because Android's recognizer delivers a whole utterance only at the END of a round — a program that steals focus mid-sentence does not misplace a character, it takes the window half an hour of speech was meant for. The watcher sleeps while the phone is away (those windows belong to the desk again) and never defends the DESKTOP pin (no fence there — fighting the whole desktop would be us stealing focus). The phone's half of the same failure: `EXTRA_PARTIAL_RESULTS` is on and `VoiceInput.partial` is a rescue copy — a round that dies without a final result (`ERROR_CLIENT`, a network drop) types what it already heard instead of deleting it, and `deliver()` is the only exit so nothing is typed twice.

12. **A gamepad press IS a button press** (build rounds R6/R7, owner spec 2026-08-07). The controller pairs with the PHONE and the WebView does not reliably expose the Gamepad API, so the Android shell captures every `KeyEvent`/`MotionEvent` (`android/.../Gamepad.kt`, claimed at `dispatchKeyEvent` before the WebView's own D-pad focus handling can fight it) and hands them to the page as `__padButton` / `__padAxis` / `__padInfo` — the shell adds only what a browser cannot. **The MAPPING lives on the page** (`client/gamepad.js`) on the existing protocol: the server needs nothing new, and a mapping change ships with the PC's page instead of a new APK. Every mapped button goes through **`buttonPress()` in controls.js — the same activator object a finger's `pointerup` runs** — so CLICK/HOLD (`press {button, down}`), the pointercancel rescue and `keepFocus` can never have a second copy to drift from; constraint 9 is why. D-pad = the LEFT group's four buttons BY SLOT (so it survives `order_land`/`order_port`) and holding holds the PC button; △◻○✕ = the RIGHT group's; L2/R2 = Layout (+)/Hide; L1/R1 **held** opens that side's category wheel and a stick POINTS at the set (release confirms, releasing at nothing changes nothing), while a short TAP steps the layout bar; left stick steers on a deadzone-plus-power curve (0.15 deadzone clears stick drift, exponent 2 so half tilt is a quarter speed — linear is unusable for precision on 4K); right stick scrolls, vertically and horizontally (`hticks`); L3/R3 = double-click / middle click; Start/Select = Keys / the layout list. A pad press also re-arms the screen-awake timer — it is neither a touch nor a keydown, and without that a controller-only session went dark and the PC correctly packed the layout away mid-work. Gate: the pad block in `tests/test_input_pipeline.py`, fail-closed in `build.py`; it pins the curve's SHAPE, never its numbers, because the owner tunes those on the real controller.

13. **A layout's arrangement is MEASURED, never remembered** (owner report 2026-08-07, the third round of the Move handle). `Layout.arranged_ratio`/`arranged_pos` were a note of what was COMMANDED, written before `place_window` was even called — so the moment a member left its rect (the app re-laying itself out, a restore out of the taskbar, a Windows snap, a placement that quietly did not take), the note became a lie and every later Apply of the SAME position matched it and re-placed NOTHING: the phone's panel moved and the PC never did again ("uvek ostavi centrirano"), which is why retrying never helped him. `LayoutRegistry.focus` now computes the targets fresh every time, asks `_standing()` where the windows REALLY are (`grids.at_rect`, ±8 px — the same tolerance `wait_landed` uses, so a min-size app that legitimately sits larger is not re-placed forever), and writes the note only when the placement LANDED; a refusal is logged by name, toasted to the phone and retried on the next focus. Gate: the placement-RECT checks in `tests/test_layout_protocol.py`, fail-closed in `build.py` (0f/6). The process lesson is mechanical: two rounds closed this while it was broken because one measured a pure function's return value and the other measured a JS variable, and the gate's own fake `place_window` threw the commanded rect away — a check on a value the user cannot see proves nothing about a feature he judges by geometry. Round FOUR closed the other half of the same lesson (owner decree 2026-08-09): even a MEASURED, landed placement was the wrong SCREEN — the server crops the region and streams the same picture wherever the windows sit inside the monitor, so moving them by `pos` changed nothing on his tablet. `pos` now anchors the letterboxed picture ON THE PHONE (`client/view-anchor.js`; gate `tests/test_view_anchor.py` computes the geometry HE judges), the server always centers the windows, and `arranged_pos` is gone — placement depends on `arranged_ratio` + `place_pending` + `_standing`, and a pos-only Apply re-places nothing while its `layout_state` still carries the anchor to the phone.

14. **The dictation types WHILE he speaks, and only what has SETTLED** (owner decree 2026-08-08: *"ne dopuštamo da on čeka dok ja stanem sa govorom, već da namjerno izvlačimo iz njega tekst"*, and the reason — *"mogu da pričam 10 minuta bez prestanka a onda ne upiše ni 1 reč... usred toga dođe do prekida... nestane sve što sam do sada pričao"*). An Android recognition round lasts as long as he keeps talking, so the rescue copy of constraint 11 only ever saved a round that DIED — a round still alive held ten minutes of speech hostage, and locking the phone or taking a call discarded all of it. Every partial is now forwarded to the page (`VoiceInput.onPartialResults` → `stream()` → `__voicePartial`), but **nothing may simply be typed as it arrives**: a recognizer REVISES its guess ("je" becomes "jeste" two words later) and we type into a FOREIGN application through `SendInput` — no composing region, no undo, **no way to unsend a word**, which is exactly what Google's own keyboard can do inside its own field and we cannot do inside his. So a word goes out only once it is **outside the held tail AND unchanged since the previous partial** (`client/voice.js` — `VOICE_HOLD_WORDS` is 3: the engine rescores the phrase it is still forming, so revisions land in the last one to three words, and three is also the length of the re-heard boundary fragments of the round-boundary trim below, so a shorter hold would let a duplicate out before the overlap can be seen). What that buys is his actual requirement: an interruption costs **the held tail, ~1.2 s of speech, never the monologue** — everything before it is already on the PC, and the round's end (final, or the rescue of a round that died) flushes the rest with what streaming already sent trimmed off the front. A final that CORRECTS an already-typed word is DROPPED, not applied: blind backspaces would eat whatever the PC caret is really sitting in. `onBackground()` still cancels everything and clears all state (the LOCK rule) and deliberately does NOT flush — the page has already closed the socket by then, so a flush would record the words as sent and lose them for good. The rules live on the PAGE, in a module kept PURE so the gate runs it whole: `tests/test_voice_dedup.py`, fail-closed in `build.py`, driving realistic partial SEQUENCES with a revision planted mid-stream — a rule about revisions cannot be proven by one call, and this repo has no JVM test runner, which is how the same feature shipped half-done before.

15. **We never estimate how long another program needs — we watch for the condition** (owner ruling 2026-08-12, after asking exactly the right prior question: *"odluka zavisi od 1 faktora a to je da li stvarno nemamo nikakav respons ... što čisto sumnjam"* — lang-ok: owner quote). His ruling is general and not about one file: *"mi da procjenjujemo koliko nečemu treba da se učita je nešto što nije dozvoljeno ni na tom ni na bilo kojem drugom mjestu"* (lang-ok: owner quote). A fixed sleep placed where another application's UI is expected to have caught up is a guess that is wrong in both directions at once — too long on his fast PC (dead time he watches) and too short on a slow app (a failure he then reads as a wrong layout, because tab extraction falls back to the whole window). Every such sleep in `server/uia.py` is now a `_poll()` on the observable condition — the context menu really holding its item, the Explorer address band reading an existing path stable across two reads, the raised window really being the foreground one, the tab count really dropping, the torn-off window really existing and its rect really standing still — and the old number survives ONLY as the give-up point. Where a timeout is reached the code refuses rather than proceeds on hope: the `Ctrl+W` that closes the extracted tab is SKIPPED and logged if the source window never took the foreground, because closing a tab in a stranger's window is far worse than leaving one open. The one exemption is named in place so nobody "fixes" it: the 8–40 ms micro-sleeps inside `_click`/`_held_drag` are the physical pacing of an injected gesture — a real held-button drag needs real milliseconds between its own synthetic input events to register as a drag at all — and they are not an estimate of anything.

16. **There are two loading animations, and which one is used depends on what is happening BEHIND it** (owner decree 2026-08-12, and he named both: *"1. vrsta je loading full screen / 2. vrsta je loading samo CUBE bez background"* — lang-ok: owner quote). `LOADING_FULL` is the opaque veil and is used where the PC's own picture is about to change in a way he must not watch — windows climbing out of the taskbar, a grid being built, a layout torn apart. `LOADING_CUBE` drops the veil, shrinks the cube to a badge and puts the label in a bordered pill of its own, and is used where we are only WAITING FOR AN ANSWER or watching something open that is worth seeing open; his own example is Chrome, *"na taj način će korisnik znati da se dešava učitavanje ali će i vidjeti da se u pozadini otvara"* (lang-ok: owner quote) — there is nothing ugly to cover there, only time to account for, and covering it would hide the only progress there is. **Both are the FRONT and gate nothing behind them** (*"loading animacija je uvek FRONT i nikada ne treba da zavisi od nje ono što se dešava iza"* — lang-ok: owner quote): no server path waits on the overlay. Both still block taps — bare is transparent, not inert. Every call site passes its kind explicitly AND carries a matching `// LOADING: FULL|CUBE — why` comment; the gate `tests/test_loading_kind.py` (fail-closed in `build.py`) holds the two to each other, because a stale inventory comment is worse than no inventory, and that inventory is what the eventual move to the shared **Loading Cube** gadget will be read off. The overlay had never been PHOTOGRAPHED in this project's life until this round — staging it in the phone audit immediately found two defects in the shape that had shipped all along (the label sitting on the cube, because a rotated 110 px cube overflows its own box by ~22 px and ate the gap; and a truncation tooth that had never met a PROGRESS ellipsis, now declared with `data-in-progress` rather than quietly exempted).

## Protocol

- Client → server (JSON text): `auth {token, screen {w, h}}` (`screen` = the device's size; its aspect drives layout window sizing — tablet vs phone), `pointer_down/pointer_up {x, y, button}`, `click {button}` (down+up at the CURRENT cursor position, no coordinates), `press {button, down}` (owner 2026-08-04 — the CLICK/HOLD mouse buttons Click/Right/Middle: DOWN when the finger lands on the button, UP when it lifts, at the current cursor — a tap is a click, a held finger holds the PC button like a real mouse; `button` is `"left"`/`"right"`/`"middle"`/`"x1"`/`"x2"` — the last two are the SIDE buttons Btn 4 / Btn 5, owner 2026-08-05, reserves in the Mouse pool), `pointer_move {x, y}`, `scroll {x, y, ticks, hticks?}` (`hticks` — owner spec "scroll vertikalni i horizontalni", build round R6/R7: the optional HORIZONTAL wheel amount beside the vertical `ticks`; absent means zero, so every client before this round — and the finger's own Scroll mode, untouched — keeps injecting exactly one vertical `MOUSEEVENTF_WHEEL` and nothing sideways; positive = right, matching Windows' own `MOUSEEVENTF_HWHEEL` sign, and it is what the gamepad's right-stick horizontal axis drives), `viewport {x, y, w, h}` (JPEG mode only — H.264 ignores it: its crop is decided SERVER-side by the focused layout, see `stream_region` below; owner order 2026-08-12, his words "zašto bi telefon dekodirao nešto što ne vidi" — lang-ok: owner quote — replacing the full-frame-always rule that cost a full 4K@60 decode for a quarter-width layout), `key_text {text}`, `key_special {key}`, `chord {chord}`, `monitor_switch {}`, `screenshot {x?, y?, w?, h?, paste?}` (the Attach set's Shot sends the REGION the phone currently views — zoom / layout focus, never the whole desktop — plus `paste:true`, and the server crops, fills the clipboard and injects Ctrl+V itself; the legacy snap sends neither), `client_log {text}` (silent phone-side diagnostics — voice evidence — written to the server log, owner 2026-08-05), `tts_info {voices}` (build round R2, 2026-08-07 — once per connection, every text-to-speech voice THIS phone has, as `{name, label, locale}`; the PC cannot enumerate another device's TTS engine, so this is the only source the desktop Settings window's Voice dropdown can have, and the list is held in memory for the run, never persisted), `paste_text {text, enter, focus?}` (owner 2026-08-05 — a TYPED command button: the server puts the text in the clipboard, injects Ctrl+V and presses Enter unless `enter:false`; built for the Claude set, whose commands are slash commands written into the app's own prompt, not shortcuts — the clipboard route is one atomic insert, and a character-by-character type races the autocomplete menu that re-filters on every keystroke. **`focus:"claude"`** (owner GO 2026-08-11, task 200 — his order that a command must not require a hand-focused prompt): OPTIONAL, sent by the Claude/Claude-Tools sets; before pasting, the server FOCUSES the Claude prompt by driving the extension's own "Claude Code: Focus input" through the Command Palette (Ctrl+Shift+P → paste the command name → Enter — state-independent and idempotent; Ctrl+Escape was rejected as a focus/blur TOGGLE, a coin flip), refusing with a toast and ZERO injections when the guarded target is not VS Code, re-running the focus guard before every injection, and withholding the Enter if the fence is lost mid-sequence (`server/content.py` → `focus_claude_prompt`; contract + honest limits in `server/__about/claude_api.md` — a non-empty editor selection becomes an @-mention typed before the command, and a pending permission dialog takes the focus instead; both predate this feature). Gate: `tests/test_claude_focus.py`, fail-closed in build.py), and the layout messages (Phase F+ step 1): `layout_pick {x, y}` (one armed tap — a grid takes one tap per cell), `layout_list {}` (the list creation source: the server enumerates every window AND its content tabs), `layout_recent {}` (task 228, owner report 2026-08-11 — the FOURTH creation source, "Recent" on the birth radial: asks for the server's persisted creation log — every layout ever created on this PC, across restarts, deduped by member set — answered `layout_recent {entries}`, `[{id, name, project, count}]`, most-recent-and-most-used first), `layout_recent_use {id}` (taps one history entry; the server re-matches its remembered members — process + a fuzzy title match, `server/layout_history.py` `match()` — against whatever stands open RIGHT NOW, never a stored handle; a member with nothing standing for it is NAMED in a toast, never silently dropped, and the layout is built from whatever WAS found (one member is enough) and refused only when NOTHING was; apps are never auto-opened to fill a gap — "Do not auto-open apps in v1". Gate: `tests/test_layout_history.py`, fail-closed in `build.py` (0al/6)), `layout_create {slots, mode, grid, orient, name?}` (`slots` = ordered cells `{hwnd, tab|null, x, y}`; `name` is the owner's own name for the layout — owner 2026-08-05, the creation panel prefills the target window's title and an empty string keeps it; every tab slot is EXTRACTED into its own window first — app's own menu command → Explorer path → SendInput drag; every failure falls back to that slot's whole window), `layout_focus {index}` (−1 = full desktop, which also MINIMIZES every layout member so the desktop shows only non-layout windows), `layout_remove {index, close?}` (owner 2026-08-08 — the bar's ✕ wore TWO acts and only did one: "brisanje layouta ga samo obrise iz nase liste ali ostavlja prozor na desktopu. Nekad hocemo to, a nekad hocemo bas da zatvorimo sve tu." It now opens the same side-by-side chooser the creation flow uses, and `close:true` also asks every member window to close — `WM_CLOSE`, POSTED, which is exactly what the window's own ✕ does, so an app with unsaved work puts up its own dialog and nothing here ever reaches for TerminateProcess. The flag is read as `is True`, never truthiness, and its absence is the harmless act: of the two only one cannot be undone from the phone, so a message that lost the field on the way must land on the one that leaves his windows alone. Members leave the topmost band BEFORE they are asked — a save dialog is a separate window and must not open underneath the thing that asked the question — and a window still standing after 2.5 s is REPORTED to the phone by count, never shrugged off. Gate: the four ✕ checks in `tests/test_layout_protocol.py`, each proven by planting its own defect), `layout_member_remove {index, member, grid?}` (owner request 2026-08-09, task 165, in translation — each row has a rename button and an aspect button, "but there must be a button by which I can throw ONE member out of the grid — to enter the grid state and remove any member, i.e. change it to a single or to a 2-grid": a four becomes a three, a three a two, a two a single. Until this round a grid could only be BUILT (`layout_merge`) or removed WHOLE, so losing one window of four meant deleting the layout and making it again. `member` is the ORDINAL of the cell he tapped, never a handle — the phone is never told a handle, and cell *k* of the row's drawing is member *k* (`client/grid-icons.js`), which is how he picks by pointing at a square. **It is NOT a close**: only the ✕ chooser closes windows, and only when he asked (2026-08-08, task 116) — the window leaves the layout, leaves the topmost band (constraint 10: a window we raised must never be stranded always-on-top, which is exactly what the LEDGER exists for), gets its normal minimize/restore animation back and goes on standing where it stands, the same "no auto-return of windows" rule `layout_remove` follows. Removing the LAST member removes the layout, through the existing `remove()` path and not a second teardown. `grid` is optional and names the arrangement a four landing on a three should take — the ONLY real choice in his catalogue, since a 2 and a 4 have one arrangement each and a 3 has four (his sheet, `UV/grid_variations.png`, 2026-08-07); unnamed or wrongly sized falls back to `3-top`, the sheet's first drawing, via `LayoutRegistry._template_for` — the one function `merge` uses too, so growing and shrinking can never disagree about what a three is. The survivors are RE-ARRANGED by the focus that follows: three windows still standing in a 2×2 is not a three. Gate: `tests/test_layout_member.py`, fail-closed in `build.py` (0t/6), each of its nine defences proven by planting its own defect — including one check written at the METHOD's own boundary because planting proved the end-to-end ones could not see it, `focus`'s own `prune` and `_standing` masking two of the promises. **The PHONE's half arrived 2026-08-09** (tasks 164 + 165, wired together because one is the door to the other): every list row now DRAWS its layout's shape — `gridIconSvg(members, grid, orient)`, three fields `layout_state` already carried, so nothing on the wire changed — and tapping that drawing opens the member chooser, one row per member, each badged with the same grid and THAT cell lit. He points at a SQUARE instead of reading four nearly identical VS Code titles; the title beside it is `member_titles[k]`, elided to one line like every other row (task 163). A SOLO layout draws the same picture as a plain `<span>`: it has nothing to throw out, and a button that does nothing is a promise the panel cannot keep. The 4→3 arrangement is asked of the RESULT (`gridIconChoices(members - 1, null)`) — a three shrinking to a two has nothing to decide. Gates: `tests/test_grid_icons.py` (the list really calls the drawing, keyed by all three fields — a pure module nobody calls is a feature that does not exist, the actions.json lesson of 2026-08-07) and `tests/test_layout_audit.py`, whose `__memberCells` fails when two member rows light the same square), `layout_member_list {index}` (task 195, 2026-08-11 — the ⚙ sheet's "Add a window": the same enumeration `layout_list` runs, answered as a `layout_offer` tagged `add_to`, windows already in ANY layout left out), `layout_member_add {index, hwnd, tab, x, y, grid?}` (grows a layout by one member through `LayoutRegistry.add_member` — the SAME `_template_for` that shrinking uses, so growing and shrinking can never disagree about what a three is; outcomes gone/full/duplicate are TOASTED, never silent; the row is absent at four — a button that does nothing is a promise the panel cannot keep), `layout_split {index}` (task 197a — the grid comes apart into one SOLO layout per member, in place, per-member `sources` preserved so ⭐/dependents stay truthful), `layout_member_eject {index, member, grid?}` (task 197b — ONE member leaves into ITS OWN new layout, never the desktop; the contrast with `layout_member_remove`, which leaves the window standing outside every layout; gates `tests/test_layout_grow.py` + `tests/test_layout_decompose.py`, fail-closed in build.py, the topmost promise checked at the METHOD boundary because `layout_focus`'s own drop-pass masked the end-to-end check — the test_layout_member lesson repeated), `actions_update {set, active, order_land?, order_port?}` (owner 2026-08-04T18:27 — dated, task 218b: the phone edits a set's INTERIOR, the same job the desktop Controls editor does, from the same Settings→Sets panel; pool listed, ≤4 `active` by ID, arrangement DRAWN as the live D-pad with tap-to-swap (never drag — it failed on his device three times); saved THROUGH THE PC into the SAME actions.json (`server/actions_api.py`, which now owns the wire whitelist `_load_actions` + `merge_shipped_pools`, moved off web.py by responsibility): ids validated against that set's own pool, any key outside `PHONE_EDITABLE` refuses the message WHOLE, `PHONE_EDITABLE` is asserted at import to be a subset of `OWNER_SET_KEYS` so a next release can never delete a phone choice, and `actions` is re-broadcast so the wheel changes with no reconnect; `enabled` deliberately excluded — composition stays the picker's job; gate `tests/test_set_editor.py`, 0ah/6), `auth` also carries `quality` (task 203 — the saved quality rides the FIRST message, so the encoder is built right ONCE instead of default-then-rebuilt; `config.quality_override()` makes the later restatement compare equal), `layout_rename {index, name}` (owner 2026-08-05 — renaming an existing layout from the list's pencil; nothing on the PC moves), `layout_reorder {source, before}` (a row dropped BETWEEN two others — the list's own order, nothing on the PC moves, owner 2026-08-07. **The focus rides on an INDEX, and a reorder moves indices** — found 2026-08-09, two days after the handler shipped: `conn["active"]` is a plain position, so re-ordering while a layout was focused left the server calling a DIFFERENT layout active, and the phone framed one layout while the bar's ✕ would have offered to close another one's windows. Corrected by IDENTITY, not by arithmetic — `reorder` never drops a layout, so the object that must stay focused is right there to be found again, and an index recomputed a second way is a second thing to keep in step. Gate: `tests/test_layout_drag.py`, which asserts the survivor by identity and never by number, because "active is still 1" was true all through the bug), `layout_aspect {index, w, h, pos?}` (owner 2026-08-03 — that layout's own W:H, `0/0` = back to the phone's own shape; the region may only ever be made SMALLER than the phone's shape: portrait keeps the full width and loses height, landscape keeps the height and loses width; `pos` 0–1000 — owner 2026-08-05, the resize panel's center Move handle (a DRAWN four-way arrow — `ICONS.move`, never a font glyph: the ✥ character came out a blunt cross on the owner's phone, owner 2026-08-05 — drag along the free axis, double-tap re-centers) — anchors the letterboxed PICTURE along the free axis ON THE PHONE, 500 = centered. **The server always centers the windows on the monitor** (owner decree 2026-08-09, the Move handle's FOURTH round — three rounds moved windows along the monitor's free axis and every gate measured them there, a screen he never sees, while the server crops the region and streams the same picture wherever the windows sit: his tablet stayed centered every time; `pos` is still received, stored and echoed in `layout_state`, and acts only in `client/view-anchor.js` — gated by `tests/test_view_anchor.py`, which computes the geometry HE judges)), `layout_grid {index, grid, orient}` (task 175, 2026-08-09 — an EXISTING layout's arrangement and orientation change in place: the server re-places the members through the same `_template_for` templates and the next `layout_state` re-locks rotation; the message had existed since 2026-08-07 with NOTHING in the repo driving it — "the server already has it" was a claim about a name — so the gate `tests/test_layout_shape.py`, fail-closed in `build.py` (0u/6), asserts the RECTS really move and that `orient` is never ignored), `next_input {}` (UIA-cycled focus through TEXT-INPUT fields — full desktop → all visible windows, layout focus → that layout's members only; built for dictation), `claude_state {}` (owner ballot 2026-08-11, task 208 — the phone asks "what is this conversation running NOW" when a Model/Thinking panel opens; the server answers `claude_state {model, model_id, effort, mode, saved:{model, effort}}` read from the TAIL of the focused layout's newest live transcript — `message.model` + top-level `effort` off the last assistant record, `permissionMode` off the last user record that HAS the field; every live field independently nullable and rendered as "unknown", never guessed; `[1m]` stripped for the family but kept in `model_id`; desktop focus = nulls with `saved` standing; the panels wear three chips SAVED / NOW / LAST SENT because three different truths exist — `/model` saves a default, `/effort` is session-only and the extension's own Modes slider re-asserts itself at every conversation relaunch. Gates: `tests/test_claude_state.py` + `tests/test_claude_panels.py`, fail-closed in build.py), and `quality {fps, res, bitrate}` (owner 2026-08-05, replacing the full/reduced cycler: the Quality button opens a PANEL — FPS Max/10/15/30/60, resolution full/⅔/½ (half PER AXIS is quarter pixels, so the middle step is ⅔ — owner), bitrate high/mid/low as percentages of the desktop bitrate — **and the PERCENTAGE rule was never his** (his correction 2026-08-12: *"što se tiče ovih izjava da su telefoni procenti od onog što koristi kompjuter, to ja nisam rekao, to si ti negde stavio kao moje reči"* — lang-ok: owner quote). It was written into this file as an owner decision of 2026-08-05 and it is not one; the record is corrected here rather than quietly dropped, because a rule wearing his name is argued with differently from a rule wearing ours. What it cost is on the record too: it is the reason the desktop's Data saver step and the phone's own auto-on-cellular level stopped agreeing when the base moved 12M → 6M, and the reason that mismatch was documented as unavoidable instead of simply fixed — the PC's card is a DEFAULT, not a wall (task 131, owner decision 2026-08-09, built 2026-08-11): lowering is free as before, and the phone may also RAISE — fps steps above the PC and a "Native" resolution step wear a ↑ and the panel SAYS the picture will blink once (raising rebuilds this client's own capture/encoder; safe because one device at a time, 4409); bitrate cannot raise — its steps are percentages of the PC's own choice by his 2026-08-05 rule, so no number above "High" exists (gate `tests/test_quality_raise.py`, incl. the check that LOWERING never touches capture). Below the ceiling the panel still SAYS what the PC holds (PC's live values in the header, unreachable fps steps struck through) — plus a "save data on mobile networks" checkbox; the client sends the EFFECTIVE values — auto-on-cellular applies the saving profile via the shell's `Android.transport()` bridge — per client via its own ffmpeg, restated on every connect; legacy `quality {reduced}` still maps to the saving profile). **Presence** (owner 2026-08-05, hardened the same night after the second failure): `hb {net?}` every 4 s while the page is visible, and `away {reason, net?}` as the parting word when the page hides. Layout members are always-on-top while the phone shows them, so the server MUST learn the moment the phone stops working — and a locked phone rarely manages a clean socket close (its Wi-Fi sleeps, the connection just goes quiet; the live symptom was the owner sitting down at his PC under hovering layout windows). **The reason is never guessed** — `reason` comes from the Android shell via `Android.hideReason()`, the only component that can read the screen/keyguard state and know whether IT launched a picker: `"lock"` (screen off or device locked — ALWAYS the end), `"excursion"` (image picker, camera, voice, permission dialog — the owner is still with us) or `""` (switched away/closed — the end); anything the server does not recognise is a LEAVE, because the safe default is the owner's own desk. The page's own 90-second timer WAS the bug: it armed on every Mic/picker tap, so locking the tablet six seconds after dictating was announced as an excursion and the layout hovered over his desk for the full 300 s backstop — the server log dated it twice, to the second (18:41:56 → 18:46:56, 18:43:11 → 18:48:11). Silence of 12 s, or any `away` that is not an excursion, ENDS the session: every layout member leaves the topmost band and is minimized, exactly like choosing Desktop, and the whole ledger (below) is released with it. An excursion holds the layout for at most 45 s — and **THE DESK WINS**: real local input on this PC while the phone is away means the owner is sitting HERE, and the hold ends at once whatever the phone claimed on its way out. Every `away` also PAUSES the stream, so nothing is encoded or sent to a phone that has gone. `net` (both messages) carries the phone's own Android `TrafficStats` counters — this app's UID and the whole device — and the difference across an absence is what the desktop **Traffic** window reports as "what the app spent while it was away": measured by the phone, not inferred by us. The layout last used is remembered SERVER-side and the next connection RESUMES there, never on the desktop (only a deliberate Desktop choice makes the desktop the resume state). Coordinates are always 0–1 within the displayed monitor. **One device at a time** (owner 2026-08-02): a new authenticated socket closes the previous one with code **4409**, and that client must NOT auto-reconnect (deliberate tap only) — two devices would otherwise steal the session in a loop.
- Server → client (JSON text): `config {monitor_width, monitor_height, stream, codec?, stream_region?, tailscale_url, app_version, base}` — `stream_region` (owner order 2026-08-12) is the monitor-normalized rect the H.264 stream COVERS when the encoder crops to a focused layout (see Region streaming, H.264 below); absent = full frame — `base` (owner 2026-08-05) is the DESKTOP Settings card as the phone must read it (`{fps, width, height, bitrate, bitrate_mid, bitrate_low}`): the desktop sets the base and the phone's quality panel may only go BELOW it, so the panel states the PC's live values and strikes out every step that cannot take effect. Without it the phone showed "30 fps" lit under a 10 fps PC and the bitrate steps were absolute constants that discarded the PC's own choice the moment the phone left "High" — which read as "the desktop settings do nothing"; `h264_bitrate_mid_pct`/`_low_pct` make Mid/Low percentages of the desktop bitrate — and `ui` (build round R3, 2026-08-07; CORRECTED to three independent axes 2026-08-08) is **how the phone LOOKS, decided on the DESKTOP and nowhere else** (his answer P4: one source of truth, no menu on the phone, no auto-detection, the device's own dark/light preference deliberately ignored): `{theme, colored, fill, colors}`. His own words on the correction: *"teme postoje samo dve, svetla i tamna, i to je onaj switcher sunce mesec. zatim dalje pričam samo za ove komande sa kojim komuniciramo sa aplikacijom i za onaj kružni meni koji biramo — on može da bude obojen, neobojen, i može da bude transparentan ili pun. dakle to je ukupno osam kombinacija za ove dugmiće: tamna tema, svetla tema, puno, prazno, obojeno, neobojeno."* The 2026-08-07 shape had folded the colour axis into a fourth `theme` value (`"colored"`/`"colored-light"`), which rendered the same eight looks by accident but claimed the page has four themes when the owner's own model is two themes (`theme`: `"dark"`/`"light"` — the sun/moon switcher) plus two switches that belong to the D-pad groups and the radial wheel, not the page: `colored` (`true`/`false` — do the controls wear each set's own colour) and `fill` (`"transparent"`/`"full"`, unchanged — reaching the same surfaces `colored` does and no others: the controls, the sets/wheel, and the always-visible on-screen buttons — Layout, Hide — never a panel or a card). `colors` is the per-set palette — ONE table, worn on BOTH themes (owner correction 2026-08-08, a day after adopting the two-table split above: *"nema dve verzije za obojene setove. Oni ce uvijek imati ove jake upecatljive boje. ono sto se menja su ostali elementi light ili dark temi ali kontrole i setovi ce biti obojeni."* — a set's colour is its IDENTITY, and an identity that changes when he flips the sun/moon switch is not one; the two-table reasoning had been right about the JOB a colour does on each page — body of the button on dark, ink on light — and wrong about what he actually wanted, and a second table was one more thing to keep in step, exactly the class of bug this project keeps meeting); the exact fourteen hexes live in `server/config.py` → `SET_COLORS` (Claude Tools joined 2026-08-11 — a set WE ship must be in the table, else theme.js hands it the next unclaimed hex and, with all claimed, the FIRST one: that is how it wore Mouse's exact colour) (`SET_COLORS_DARK`/`SET_COLORS_LIGHT` still exist, as aliases pointing at the same table, so an old import cannot quietly resurrect a second one — `set_colors(theme)` still takes `theme` and deliberately IGNORES it, kept only as the record that the question was asked and answered with "it does not matter"), not duplicated here since a second copy is exactly what goes stale; a CUSTOM set takes the next colour of that palette nothing already wears. When `colored` each set's buttons carry its colour — outline AND ink when outlined, fill when filled — and the ink is **computed from the colour's luminance, never tabled**, so the phone audit's contrast tooth stays green whatever the owner retunes. The page writes it straight into CSS variables (`client/theme.css` / `client/theme.js`) and caches it per device against the first-paint flash; a server or a device cache still holding the pre-2026-08-08 four-value `theme` is TRANSLATED, never reset (`config._migrate_legacy_ui` / `client/theme.js` → `legacyTheme`) — the owner's saved choice must never silently become something else. It arrives with the next `config`, so a change reaches the phone on its next connection, not live — `hand` (`"right"`/`"left"`, desktop Settings) is legacy — ignored by the client since the offset system was dropped (2026-08-02) — after auth and after EVERY stream (re)start (monitor switch, H.264 session reset); the client must fully reset its view and decode pipeline. `stream` is `"h264"` or `"jpeg"`; `codec` (H.264 only) is the exact MSE string (e.g. `avc1.4D4028`) parsed server-side from the live init segment's `avcC` box — never guessed; `tailscale_url` is the works-anywhere address with token (null until the PC is signed in to Tailscale) and feeds the client's guided wizard; `app_version` is the server's version — inside the APK the page compares it with the shell (`Android.appVersion()`) and offers the in-app update banner. `actions {sets}` (radial-wheel shortcut sets from `actions.json`, after auth — and re-broadcast live after every `actions_update`; the frame also carries top-level `wheel_order` and `wheel_mode` (task 181, owner decree 2026-08-11: `"dropout"` default — a set PLACED on a D-pad group drops off the category wheel while it rides, capacity rises to 10, ranks close along wheel_order; `"fixed"` keeps today's wheel at 8, tucked in the desktop Controls editor's "Wheel mode" beside "Wheel order…", never phone-editable; the no-duplicate rule — one set never on both sides — ships in BOTH modes; owner-owned across the merge via `OWNER_TOP_KEYS`; gate `tests/test_wheel_dropout.py`, and the input gate caught the pad's pick mapping the FILTERED ring's index into the full list on day one — the pad now maps through `wheelCats` exactly as the finger does)), `clipboard {text}` (task 182, owner order 2026-08-04 ~17:05 — everything copied lands on BOTH clipboards: sent after an injected Copy/Cut (the server reads the PC clipboard with a settle-retry — SendInput returns before the app fills it) and whenever the live PC-side `AddClipboardFormatListener` (new `server/clipboard_sync.py`, focus_hook's listener-thread shape, ONE eternal WNDPROC thunk because RegisterClassW keeps the class process-wide and a per-run thunk was a hard 0xC000041D on the second session) sees a change while a session watches; the page hands it to the NEW bridge method `Android.setClipboard(text)`; a push while the page is hidden is HELD latest-only and flushed on the return — never dropped, never stale-over-newer; echo-guarded both directions; text only for now; gate `tests/test_clipboard_sync.py`), `toast {text}` (user-facing notice shown on the status pill), `notify {agent, event, title, text, speak, voice, rate, speak_text?, layout?}` (`speak_text` — owner slika-4 report 2026-08-11, task 224: the VOICE says only "<project> — <title>", never the body; the banner keeps title+body unchanged; built server-side in `notify.speak_summary` so every carrier — page, waiting channel, queue — speaks the same; the page and `NoticeService.kt` both fall back to the old sentence when the field is absent, so an old server keeps working) (owner 2026-08-05, ROADMAP Phase H — a job on the PC finished and NAMES ITSELF: the phone raises a real Android notification tagged with the agent, speaks it aloud, and toasts if the page is visible. Fed by `POST /notify?token=…` from anything on the PC that can run a command when it finishes — `setup/agent_hook.py` is the Claude Code `Stop` hook. The agent's name is the point: several agents run at once, so a bare beep says nothing. A notice for a phone that is NOT connected now WAITS (owner 2026-08-06 — two agents finished while he was on a call with the app closed and both were discarded): at most 30 minutes and 20 deep, delivered oldest-first on the next connection, and the phone appends "8 min ago" so a held notice never pretends it just landed. The hook says **"needs you"**, not "finished": a Claude Code `Stop` fires at the end of every TURN, including mid-job when the agent stops to ask something, and what is certainly true then is that the next move is the owner's. Android's POST_NOTIFICATIONS is asked for once at app START, never when the first notice arrives — asking then spent that notice on the dialog, and in the background the dialog cannot even appear. **HOW it is said is the DESKTOP's decision** (build round R2, 2026-08-07): the Settings window's NOTIFICATIONS card owns "Say it out loud" (off sends `speak:false` and nothing else — the Android banner still appears, so muting one carrier never loses a notice), the `voice` (a `Voice.name` exactly as the phone reported it in `tts_info`; a device that no longer has it falls back to its own default, so a phone swap can never silence the feature) and the `rate` (0.8 / 1 / 1.25 / 1.5). All three ride on EVERY frame rather than being pushed once, so there is no phone-side state to go stale and a reconnect cannot leave it speaking in last week's voice. The shell exposes `Android.speakAs(text, voice, rate)` BESIDE the older `Android.speak(text)` — a new method, not more arguments, because the page is served by the PC while the shell is installed separately, and a bridge method that changed arity would simply stop resolving for an older page. **HOW it is said is the DESKTOP's decision** (build round R2, 2026-08-07): the Settings window's NOTIFICATIONS card owns "Say it out loud" (off sends `speak:false` and nothing else — the Android banner still appears, so muting one carrier never loses a notice), the `voice` (a `Voice.name` exactly as the phone reported it in `tts_info`; a device that no longer has it falls back to its own default, so a phone swap can never silence the feature) and the `rate` (0.8 / 1 / 1.25 / 1.5). All three ride on EVERY frame rather than being pushed to the phone once: there is then no state on the phone to go stale, and a reconnect cannot leave it speaking in last week's voice. The shell exposes `Android.speakAs(text, voice, rate)` beside the older `Android.speak(text)` — a NEW method, not more arguments, because the page is served by the PC while the shell is installed separately and a bridge method that changed arity would simply stop resolving for an older page. **And the notice no longer needs him to be looking** (owner decree 2026-08-07: *"notifikacije mi stižu tek kada podignem aplikaciju iako je sve vreme otvorena u pozadini"*). The notice used to ride the STREAMING socket, which constraint 8 closes the moment the page hides — so at the exact moment a notice mattered there was no channel, and the 30-minute queue had quietly become the delivery path instead of the fallback. His instruction was a small service with a MINIMAL channel: *"android strana čeka signal, ne prima ništa od kompjutera, ali ostane u stanju čekanja signala."* So the APK runs a `connectedDevice` foreground service holding one idle `GET /notices?token=…&device=<id>` — a response that never ends, down which the PC writes ONE BYTE a minute (NAT timeout plus death detection; the phone writes nothing, ever, and holds no wake lock — ~150 KB/day) and one JSON line per notice, identical to the page's frame. The PC picks **exactly one carrier** per notice (`notify.deliver()`: page → waiting channel → queue, a chain of returns), so the same notice can never arrive twice and no de-duplication rule exists anywhere; a page dying mid-notice falls THROUGH to the channel instead of into the queue. **The channel is keyed PER DEVICE** (task 209, 2026-08-11, read out of his own log): it used to be a single SLOT, so his tablet and his phone kicked each other off it every few seconds — continuously since 2026-08-09, thousands of log lines a night, both radios woken for nothing, and a notice reaching only whichever device held the slot that second while the other learned of it minutes later out of the queue ("notifications sometimes never arrive"). The shell sends `device` — a random per-INSTALL UUID in its own SharedPreferences, never a hardware id — the page-visible carrier still outranks everything, and the waiting branch then hands the notice to EVERY waiting device once each: per-device de-duplication is structural, since a device has exactly one channel, and an attach replaces only its own. A request with NO `device` is an APK older than this round and keeps exactly the old single-slot behaviour, so nothing about an old phone changes when the PC updates. The kicked service also BACKS OFF now (5→15→45→60 s after a connection that ended without one beat, distinct from the 5 s of a link that really spoke and the ×2-to-5-min of nothing answering at all) — the instant retry was what turned a kick into a ping-pong. **A waiting channel is never a present phone, structurally**: `/notices` only READS the one-device slot and never writes it, so no capture, no encoder, no `presence.watchdog`, no `focus_guard.watch`, no layout registry — a phone in his pocket can hold nothing always-on-top over his desk, and the streaming session still dies the instant the page hides. Battery-optimisation exemption is the one thing Android will not let the app take for itself: it is explained on the PAGE and granted in the system dialog. Gate: `tests/test_notice_channel.py`, fail-closed in `build.py`. Honest limit: a phone reboot stops the service until he opens the app once. **And a CLOSED app is silent BY DESIGN** (owner rule 2026-08-12: *"notifikacije su samo ako aplikacija radi u pozadini"* — lang-ok: owner quote): swiping the app out of recents fires `onTaskRemoved`, the service stops itself (`running` cleared before `stopSelf`, so a sticky restart racing the stop re-arms nothing) and the PC's 30-minute queue holds whatever finishes until the app is opened again; only backgrounding keeps the channel. Gate: the closed-app check in `tests/test_notice_channel.py`. **And the tap goes WHERE it happened** (owner 2026-08-08, task 110: *"da klikom na notifikaciju nas odvede do tog layouta … gde je zavrsio taj sabagent ili glavni agent"*): `layout` is `{index, name}` for the layout showing that agent's project, and nothing about it is inferred — the finishing agent reports its own `cwd` (`setup/agent_hook.py` → `agent_project`), every layout can be asked which project its windows really belong to (`Layout.project`), and `notify.layout_of` matches the two at SEND time, after the same prune `layout_state` numbers the phone's list with. The field is ABSENT whenever that project is on no layout — a tap that cannot land must not be offered. It rides `Android.notifyAt` (a NEW bridge method beside `notify`, for the `speakAs` reason: the page is served by the PC while the shell is installed separately, so changed arity would stop resolving), travels the notification's intent, and is PULLED back by the page rather than pushed at it — the tap may have cold-started the app, and at that moment there is no page to push to. The page then VERIFIES before moving: the NAME decides and the index is a hint, because a layout removed in between slides every higher index down and landing him in a stranger's window is worse than not moving. A tap outranks the excursion auto-restore — only one of the two is something he just did. Gate: the `layout_of` and tap checks in `tests/test_notify.py`, each proven by planting its own defect), `cursor {x, y, shape?}` (PC pointer position, normalized, sent on change ~30 Hz — DXGI capture never contains the pointer; the client draws a virtual cursor in both modes. `shape` (owner request 2026-08-09, task 142 — *"prikazi mi stvarni kursor kako izgleda, a ne da stalno bude strelica, tako da znam da tu mogu da kliknem i da promenim dimenzije prozora"*) is the NAME of the system cursor the PC is really showing — `arrow` · `ibeam` · `hand` · `size-we` · `size-ns` · `size-nwse` · `size-nesw` · `move` · `wait` · `cross` · `no` · `up-arrow` · `app-starting` · `help` · `custom` — read from `GetCursorInfo`'s live `HCURSOR` matched against the `LoadCursorW(NULL, IDC_*)` handles, resolved ONCE and cached because this runs at 30 Hz (`server/cursor_shape.py`). A resize cursor at a window edge is how a person knows the edge is grabbable, and one eternal arrow told him nothing. Rules: it rides the EXISTING message as an OPTIONAL field — never a new type, never an image — so a page that predates it ignores it; a shape CHANGE alone is a send (hovering onto an edge moves nothing and is the whole feature) but nothing unchanged is ever resent, so the cadence is exactly what it was; an app's own cursor matches nothing and is answered `custom`, never a near-miss, because a wrong shape promises a grabbable edge that is not there; and a cursor the PC cannot read at all (secure desktop) leaves the field OFF the wire. The phone draws each name as a canvas PATH with its hotspot at the shape's origin (`client/cursor-shapes.js` — pure, so its gate runs it whole; arrow-family cursors point with the tip, the resize/move/wait/I-beam family is centred), keeps the white-fill/black-outline/shadow treatment that makes it legible over light and dark PC content, and falls back to the exact original arrow for `custom` or any name it does not know. Gate: `tests/test_cursor_shape.py`, fail-closed in `build.py` (0p/6)), `layout_state {layouts, active, region, orient}` (after auth and on every layout change — the registry lives server-side for the server's lifetime; `region` is the monitor-normalized rect the client locks its view onto), `layout_recent {entries}` (task 228 — the answer to `layout_recent {}`: `[{id, name, project, count}]`, every layout ever created on this PC — persisted across restarts in `server/layout_history.py`, deduped by member SET regardless of tap order, ranked most-recent-and-most-used first; `id` is the dedupe signature, opaque to the phone, echoed straight back in `layout_recent_use`) and `layout_offer` (two shapes: the answer to `layout_pick` — `{target, tab, x, y, grids}`, one tapped slot; and the answer to `layout_list` — `{entries, grids}`, every window plus each window's content tabs as separate entries; entries carry real app icons as PNG data URIs). Each entry in `layout_state.layouts` also carries its `agents` (owner 2026-08-06 — WHICH agent tools the PC found RUNNING in that window's project: `server/agents.py` reads the process table, a live `claude.exe` carries its `--resume=<session-id>`, the session id names a transcript whose `cwd` names the project, and the VS Code title ends in that project's folder. This is what REPLACED making the owner tick "this window has Claude open": UI Automation genuinely cannot see it — the tab is named after the CONVERSATION and VS Code hides webview content from accessibility — but the process table can. An app set claims it with `"agent": "claude"` in actions.json. The honest limit: every VS Code window shares one Electron process, so the match is per PROJECT FOLDER — two windows on the same folder both count), its `member_titles` (owner request 2026-08-09, task 165 — the title of EVERY member, in CELL order, so the phone can name the window it is throwing out: it cannot ask for a window it was never told about, and cell *k* of the row's drawing is member *k*. Titles only, never icons — `layout_state` rides every focus and every change, and an icon per member would multiply the frame for a panel that opens rarely; the CELL is the picture, the title is the word. Read live on every frame, like `title` beside it, which is `member_titles[0]`), its `parent` (owner decision 2026-08-09, task 169 — the **⭐** the layout SELECTOR puts before the first letter of a layout's name: true when one of its member windows is the window ANOTHER layout's content was torn out of, so closing it would take that other layout's tab with it. His decision, as relayed in translation (task 169): the place that needs the parent marked is the LAYOUT SELECTOR — the list where Desktop is — with one emoji, ⭐, before the first letter; the CREATION list needs no mark at all, because its indentation already shows who the parent is (that indentation is task 168, and separate). Computed in `LayoutRegistry.state` off `Layout.source`, which `resolve_slot` already records at creation — no new probe and no guess from a title; a layout is never its own parent, since the source window may itself be a member of the same layout and closing that pair together surprises nobody. **An emoji although this project draws every icon** (the ✥ move handle came out a blunt cross on his phone, 2026-08-05): he asked for this one by name, and a colour emoji from Android's own emoji font is exactly what that dingbat was not. It is its own element with its `line-height` pinned to the row badge's 20 px — task 163's ellipsis can then never eat it and a colour glyph's own metrics can never make a starred row taller than its siblings. **Since task 173 (2026-08-09) every slot's source is recorded** — `Layout.sources` (member→source, fed from every resolved slot, kept in step by `prune`/`drop_member`/`merge`; `.source` survives as the first member's read-only alias) — so the mark no longer under-reports; each layout also carries `dependents`, the NAMES of every other layout whose content was torn out of a window this one holds, in list order, and `parent` is simply `bool(dependents)` — one computation, so the ⭐ and the ✕ chooser's warning can never disagree. The ✕ chooser's CLOSE option carries those names as a warning strip inside the close chip BEFORE he taps (task 171) — a filled `--warning`/`--on-warning` strip, because coloured ink alone measured 4.37:1 on the light card and failed the audit. Gates: `tests/test_layout_drag.py` builds the window/torn-off-tab pair through the real creation path and asserts all three states plus the merged self-contained case; the audit's `__layoutStars` proves the right ROW wears it, in every look, on all four screens, without changing the measured row height), its `ratio` (`[w, h]` or null) and `pos` (0–1 free-axis anchor of the letterboxed picture on the phone — 0.5 = centered, owner 2026-08-05; since the 2026-08-09 decree it moves nothing on the PC, the client's `resetViewHome` re-anchors the fitted view with it). The creation list **leaves out windows that already belong to a layout** (one window cannot be shown twice) carries the same `agents` per entry and offers tabs **only from tab-capable apps** (`uia.TAB_APPS` — an app's internal section switcher is a UIA `TabItem` exactly like a Chrome tab, and extracting it always failed back to the whole window). **A tab is offered only when its window holds MORE THAN ONE** (owner 2026-08-09, task 167: extracting a tab into its own window is only possible then), and the lone tab of a one-tab window VANISHES — it and its window are the same picture on screen, so offering both counted one window twice and a VS Code with three tabs was offered as four members. The rule lives whole in `uia.offerable_tabs`, which also refuses a **minimized** window deliberately: Windows reports it as having no size, so it enumerates zero tabs whatever it holds, and the list therefore used to show the same window with and without its tabs depending on its state — the entry carries `tabs_hidden` and the phone says `minimized` on that row. From this the phone computes **how many members the desktop can really fill** (`availableMembers` — a window is worth its offered tab count when that is ≥ 2, else 1: take k of N tabs and you hold k windows plus the original while k < N, so N either way) and **caps the 2/3/4 shape chooser by it** (owner report 2026-08-09, task 166 — it offered a grid of four while the desktop held three, and there was no cap of any kind; `entries.length` is the wrong number and was never the answer). A window and one of its OWN tabs can no longer be chosen together, a not-ready **Create** is dimmed AND says what is missing instead of silently swallowing the tap, and the server no longer builds the impossible grid in silence: `layout_api.grid_for` derives the shape from the members that ARRIVED via `LayoutRegistry._template_for` — the same function `merge` and `drop_member` ask — and TOASTS every downgrade, because three windows in a 2×2 framed four quadrants and streamed bare desktop in the fourth while every stored value looked correct (gate: the coverage checks in `tests/test_layout_protocol.py`). In BOTH of the panel's lists a tab is drawn **INDENTED under its window** and the old `"↳ "` prefix is gone (owner 2026-08-09, task 168: *the indentation stays — a column; a sub-tab does not belong to its parent's kin group, which is exactly why a minimal indent is allowed; the arrows are less noticeable and less intuitive*) — both lists became real rows for it (`client/layout-create.css`, reusing the layout list's own `.lay-item`), the indent is exactly the icon column so a tab's title lands under its parent's, and the app icon belongs to the WINDOW alone. Creation UI (task 186, owner's decided design 2026-08-10 ~20:25, built 2026-08-11 — SUPERSEDES the 158 two-option S/SE radial AND the old source-chooser card, the 221 lesson): the Layout (+) button opens ONE CENTERED radial (the sets wheel's own rule and veil layer) with three drawn one-word sources — **Tap** (fingertip icon; arm the next tap), **List** (three-rows icon) and **New** (window-with-+ icon; task 184 — a layout from a window that is NOT open yet: `GET /recents` / `POST /recents/open` in new `server/recents.py` — Explorer Quick Access folders, VS Code's cached Open Recent menu out of storage.json, and honestly only New window / Incognito for Chrome (its session files are undocumented binary); the chosen thing OPENS, never steals the fence, and only a window that was NOT standing before the launch becomes the member; the three words are still his to rename); the gamepad speaks the SAME grammar it already knows: L2 TAP arms tap-pick, L2 HOLD opens this very radial, a stick points, release confirms, release-at-nothing changes nothing (gates `tests/test_layout_birth.py` + `tests/test_birth_radial.py`). **When an app opens mid-session** (task 185): a NEW top-level window correlated with a recently injected double-click raises the `window_offer` chip in an `act:"layout_new"` variant — "layout with it?" — and a yes lands in the ordinary creation panel pre-seeded with that window; never dialogs, never steals PC focus, auto-dismisses. **The layout bar** (task 223, his slika-3 spec superseding 160's literal reading): the ‹ › arrows are glyphs with a tiny footprint, the framed name takes ALL remaining space (≥60% of the bar, gated) with small padding, the app logo hugs the left edge and ✕ the right, the name may wrap two rows, and a horizontal SWIPE anywhere on the bar steps the layouts exactly like the arrows. The old flow's remainder stands: a loading overlay covers the visible seconds of server-side tab extraction. **The loading animation lasts as long as the work does** (owner 2026-08-03, repeatedly): `layout_state` only arms a settle watcher that samples the live frame, and the overlay drops when the streamed screen actually stops moving — the user never watches windows climb out of the taskbar. It cross-fades in/out and opens on the next cube face every time (top → left → back → right → front → bottom; face colours in `style.css`). **Layout focus survives excursions** (owner 2026-08-04): server-side focus is per-connection, and every excursion that hides the page (gallery pick, permission dialog) closes the socket by rule — the client therefore remembers the focused layout (`layoutRestore`) and, when a fresh connection's first `layout_state` says desktop without the user having chosen it, re-sends `layout_focus` once (same index AND name, else skipped); a deliberate desktop/remove choice clears the memory and is never overridden. **`layout_state` is a VERIFIED claim** (owner 2026-08-04, after "stopped moving" lied twice): the server holds it until every member's frame rect actually matches its commanded rect (`wait_landed` — position, not stillness; a refusal reaches the phone as a toast, never a silent warning). **Layout members are TOPMOST from first touch until the layout stops being shown** (owner decree 2026-08-04: never below ANY other window, incl. always-on-top ones, during creation or focus); `drop_topmost` strips it on desktop focus, focus of another layout, removal, and disconnect — nothing stays always-on-top for the owner at the desk.
- **Everything is guided IN the apps — never in chat/manuals** (hard owner principle, restated angrily 2026-07-22 and binding): any step a user must perform gets step-by-step on-screen guidance with plain language and automatic continuation. Desktop: three-state Tailscale card (install / sign in / connected). Phone first contact: the **QR always encodes the LAN address**, and the FIRST scan hard-requires the phone on the home Wi-Fi (a fresh phone has no tunnel to the PC — with mobile data the LAN address loads NOTHING, just a dead dark page; hit live 2026-07-22). The PC window states this requirement under the QR — the only place that can, since an unreachable page cannot guide anyone. Opening the QR in the phone browser lands on the **install funnel** (Open the app first/primary; Install below → paired automatically via `intent://` — the browser never shows anything else). Inside the app, the client page shows an "anywhere access" banner (auto-appears ONCE per device — owner 2026-07-26; afterwards Settings → Anywhere reopens it) → in-page wizard: Google Play deep link for the Tailscale app → sign-in step → the page polls `GET /ping` (auth-free 204, reachability only) on the Tailscale address and hands over the permanent works-anywhere link the moment the phone joins. Never send the end user outside the guided flow.
- Server → client (binary), by announced mode: **H.264** — the raw fragmented-MP4 byte stream (init segment first; the client appends chunks into MSE in arrival order). **JPEG** — 16-byte header (4 × float32 LE — monitor-normalized x, y, w, h of the region the frame covers) + JPEG bytes.
- **H.264 streaming is per-client**: one shared capture feeds one ffmpeg process per connected client (fresh init segment + keyframe at connect — no mid-stream joining). H.264 bytes are never dropped individually; a client that falls a full queue behind gets its whole session reset (new `config` + stream). Capture/encode run only while ≥1 client is connected. **And a client that is GONE leaves every structure that counts it** (live failure 2026-08-07, found in his own log while he reported a juddering mouse): `await asyncio.to_thread(manager.open_session, …)` cannot be cancelled — the worker thread runs on, ffmpeg spawns, the session registers itself, and the cancelled coroutine never bound the name that would have closed it (`CancelledError` is a `BaseException`, so neither `except` caught it and the `finally` belonged to a block never entered). One such orphan ran four hours at native 4K with no phone connected: `_sessions` never empty so capture could never stop, 12,924 s of ffmpeg CPU, and 1,890 `stream backlog` warnings resetting a queue nobody would ever read — his own log printing "2 active" for one phone, and the first orphan dating to 2026-08-04. The fix is a CLAIM (`h264_streamer.SessionOwner`) made on the event loop BEFORE the encoder thread exists and released by the consumer's teardown on every way a socket can end (clean close, 4409 takeover, network death with no close frame, an exception in the send path, server stop); a mutex settles the race both ways, so a session whose claim is already released is terminated and never registered. The reset path carries a SECOND, independent rule: `push` refuses a released claim, because resetting a session with no live socket is a bug in its own right. Gate: `tests/test_stream_lifecycle.py`, fail-closed in `build.py` — it drives the real loop over the real manager with ffmpeg and dxcam faked, and each defence is proven by planting the other's defect. Never remove either half, and never store an `open_session` result the caller cannot be guaranteed to receive.
- **Controls are two configurable D-pad groups** (owner design): each group shows one category of up to 4 buttons, arranged as a D-pad cross in landscape and a column in portrait. A small dashed centre button opens a **tap-based** category wheel (tap to open, tap an item, ✕ cancels — no hold/drag). Default sets (owner 2026-08-04, see [ACTIONS.md](ACTIONS.md)): **Mouse** (Click/Right/Middle as CLICK/HOLD + Scroll; the side buttons Btn 4/Btn 5 wait in the pool — owner 2026-08-05), **Input** (Keys / Enter / Esc / Mic — Enter and Esc switch keyboard+mic OFF first; Mic = direct voice via the shell's `Android.startVoice()` SpeechRecognizer bridge (subsystem in `android/.../VoiceInput.kt`), results typed as `key_text`; only ONE of mic/keyboard is ever ON — switching the keyboard ON still switches the mic OFF — but a tap on the stream switches OFF only the KEYBOARD and the mic KEEPS LISTENING (owner 2026-08-09, amending the 2026-08-04 both-off rule: he steers the cursor while dictating, and every steer killed the mic mid-sentence; the mic stops only via its own Mic button, the Enter/Esc buttons, the keyboard going ON, or page hide/lock). **The dictation language is a USER CHOICE** (owner round 2, 2026-08-05 — round-1 evidence: pinning to the phone's FIRST system locale transcribed the owner's Serbian as English garbage, his phone lists English first; and the transient diagnostic toast was rejected angrily): the FIRST Mic tap opens the **dictation setup card** (plain language, persistent, Tailscale-card pattern — candidates = system locales + keyboard languages, LANGUAGE-AGNOSTIC, statuses "ready"/"model will download"/"online"). **That card NAMES THE DEVICE it describes, and every language on it can be HEARD before it is chosen** (owner 2026-08-09, task 127, with his screenshot: *"to je samo za Samsung a koristim dva uredjaja — jezik, ako je vec vezan za uredjaj, treba da kaze OVAJ UREDJAJ ima te i te jezike; i treba da mogu da CUJEM da bih odabrao"* — lang-ok: owner quote): every row is a fact about the phone in his hand and he owns two, so a card naming none read as if these were THE languages — the name comes from `navigator.userAgent`'s model token, refined by `navigator.userAgentData.getHighEntropyValues(["model"])` where Chromium's UA reduction froze that token to the literal "K", and falls back to a plain "this device" rather than ever guessing (no bridge method: the page is served by the PC while the shell is installed separately, so a header needing a NEW `window.Android` method would be blank on the shell he has today). Each playable row carries a speaker button that speaks one WRITTEN sample sentence in that language through `Android.speakAs` — voices from `Android.ttsVoices()`, the same list `notify.js` sends the PC as `tts_info`, matched by language subtag with the region only choosing between several. **The honest limit is the rule, not a footnote**: recognition languages and TTS voices are different sets, so a row with no voice says "no preview voice on this device" and a row with no written sample says so too, and NEITHER ever falls back to speaking English in the default voice — he would hear English, believe he had heard the language he tapped, and choose by it. One sample at a time and never a queue (the shell's `TextToSpeech` runs QUEUE_ADD with no stop, and its voice is set per call but applies to the whole queue, so a second tap would speak BOTH in the second language's voice). Both preview states plus the device line are staged and measured by the phone audit (`tests/_audit_panels.py` → `DICT_STAGE_JS`). The choice is changeable any time via Settings → **Language** (the `dictation` action REPLACED `anywhere` in the default Settings set; `anywhere` stays in the action pool — ROADMAP preset combining), a missing-but-supported model auto-downloads while online recognition serves the SAME chosen language and the Mic button wears a dashed/pulsing look (`__voiceState`), the switch to on-device is SILENT when the model lands, and diagnostics go via `__voiceInfo` → `client_log` to the SERVER LOG — never a panel on the phone. Round 4 (owner 2026-08-05): the card also holds "More languages…" (downloadable models + the online service's list, collapsed behind one row) and "Mute listening beeps" (default ON — the round tones cycle on every silence); **the LOCK button stops EVERYTHING** — the shell's `onPause` cancels the listening round and refuses new ones until resume, the page switches mic+keyboard OFF on every hide (rounds used to keep beeping under a locked screen); and the server runs the Controls-editor's shipped-pools merge ONCE AT START (`web._merge_shipped_actions`, FROZEN-only) so default-set updates reach the phone without opening the editor), **Attach** (Gallery / Shot / Camera / Files — every source ends as a paste on the PC; gallery/Files multi-select, one image = bitmap, several or non-image = REAL files via CF_HDROP + `/upload_files`; **Region** waits in the pool — owner 2026-08-05: a frame the finger sizes and moves with full freedom of size AND position, Snipping-Tool style, whose contents are captured and pasted like every other source, reusing `screenshot {x,y,w,h,paste:true}`; the old single-image `Image` left the pool, being a weaker Gallery), **Edit** (All/Copy/Cut/Paste with icons) and **Settings** (Monitor switch moved here + next_input + quality + anywhere). Zones is NOT shipped — a custom-category story. **App-aware sets** (`app_sets` in actions.json) exist ONLY in layout focus: when the focused layout's process matches (VSCode/Chrome/Explorer/**Claude** shipped), the set appears as an extra wheel category and vanishes with the focus — nothing switches by itself. **More than one may match at once** (owner 2026-08-05): a set may add a `title` test on top of `process`, which is how **Claude** singles out Claude Code INSIDE VSCode (same `Code.exe`) and rides beside the VSCode set — the ONE case where two app sets are on the wheel at once, and both are wanted. **The title test is a WORD and a document never matches** (owner 2026-08-06, shouted): the Claude set belongs to the Claude CONVERSATION and to nothing else — an open `CLAUDE.md`, a transcript, any text file carrying the word is still plain VSCode, so `title` may be a LIST of spellings, is matched on word boundaries, and a title that looks like a file name (an extension, with or without a `— App Name` tail) is refused outright. The title matched is the member window's OWN title, captured at creation (`Layout.title`), never the layout's owner-chosen name, so a rename cannot change which set appears. **But the title test is only the FALLBACK, and NOBODY TICKS ANYTHING** (owner 2026-08-06, restated 2026-08-07: *"nema potrebe da se vidi da li pravimo Claude/VSCode/Chrome/Explorer prozor jer naš program to prepoznaje"*): probing his PC with a Claude conversation open found the window titled `Ispravka UI dizajna meni… - Vibe Coder - Visual Studio Code [Administrator]` and its tab `Ispravka UI dizajna meni…, Window 2: Editor Group 1` — Claude Code names itself after the CONVERSATION, wears the SAME UIA class as the `prompt.txt` tab beside it, leaves `AutomationId`/`HelpText` empty, and a walk of its whole extracted window (20 elements) finds no "claude" at all because VSCode hides webview content from accessibility. No string can identify it — but the PROCESS TABLE can (`agents`, above), and that is the answer, read fresh on every `layout_state`. A tick list DID exist between 2026-08-06 and 2026-08-07 (`layout_create.app_sets`, `layout_apps`, `Layout.app_sets`) and it is GONE: it was written once at creation and then outranked the live answer forever (`sets.js`: "a layout that HAS the list is answered from it alone"), so a single miss froze his Claude layout on the VS Code wheel while `agents` said `claude` on every frame two lines away. Never store an answer the PC can read. **And a field we invent must be PROVEN to reach HIS file, not ours** (his report 2026-08-07, the fifth on one bug: "zašto se stalno vrtimo u krug između program zna i program ne zna"). The user's `actions.json` is seeded once at install and never replaced; `merge_shipped_pools` is its only path, and it copied a HARDCODED LIST of field names — so `"agent": "claude"` never arrived, the Claude set could only match by TITLE, and Claude Code names its VS Code tab after the CONVERSATION: unsatisfiable, forever. His own server logged `Shipped action pools merged` at 16:18:52 that day and the field still was not there. **The process failure is the bigger half: every guard built its "user file" with `user = copy(shipped)`, so it already carried the new field before the merge ran — we proved the repo's file to itself through four releases, and nobody opened `%LOCALAPPDATA%\VibeCoder\actions.json` once.** The rule is now INVERTED and forward-compatible: OURS = every key not listed as his (always from shipped, and DELETED if shipped retired it, so a dead field cannot lie to the phone); HIS = `active` / `order_land` / `order_port` / `enabled` / button `label` per set, and top-level `wheel_order` / `left` / `right` / `custom_sets` (kept if present, SEEDED if absent — a default he has never had an opinion about is not an opinion to protect). A new owner-editable key MUST join `OWNER_SET_KEYS`/`OWNER_TOP_KEYS` in the same commit that makes it editable. Gate: `tests/test_actions_migration.py`, fail-closed in `build.py` — it starts from an OLDER file shape held as literal text and plants a field name nobody has invented, because a gate naming today's field ships broken on tomorrow's. Detection is likewise no longer one measurement: `server/agents.py` reads `~/.claude/sessions/<pid>.json` (the project `cwd` outright, cross-checked against the process table by PID **and** process start time), then `--resume`, then recent transcripts bounded by the count still unnamed; `--claude-in-chrome-mcp` is not a conversation; the VS Code extension host proves HOW MANY windows run one but cannot name WHICH (its command line carries no workspace path — measured 2026-08-07). **Where each set sits on the wheel is the owner's** (build round R5, 2026-08-07): a top-level `wheel_order` array of set names, set in the desktop Controls editor's **Wheel order…** dialog (a drawn ring beside the ladder) — position 1 is 12 o'clock, the rest clockwise; a set not riding is skipped and the ring closes without a hole; a set the order does not mention rides at the end; the cap of 8 is unweakened. `_load_actions` must pass it to the phone — it whitelists fields, and the feature was a no-op for every user, fresh installs included, until that line existed. Each app set has its own tick in the phone's Settings → `sets` picker (the old single master switch stays as the group's heading). The Claude set's commands are TYPED (`paste_text`): Usage `/usage`, Model `/model`, Thinking `/effort`, plus Stop (Esc) and a Menu button that types `/` WITHOUT Enter so the command list can be picked with the cursor. A **Focus** button existed in the pool until 2026-08-13 and was DELETED on the owner's ruling — a command whose whole job is to click into a text field is not needed, and several already do it (Tab; `next_input` asks for the next text field outright). It was also wired to `ctrl+esc`, a chord Windows reserves for the Start menu, so it most likely opened Start rather than focusing anything; that is not why it went, but it is why nobody ever reported it working. `buttons` is entirely OURS, so the merge deletes it from his own file too, and `tests/test_actions_migration.py` refuses a shipped Claude set that carries a `ctrl+esc` command again. **Wheel composition** (owner 2026-08-05, revised same day): Mouse, Input and Settings are `required` — never hideable; every other shipped set (Edit, Attach, Navigate, Cursor, Media, Windows — the last two off by default) and every owner-made `custom_sets` entry (created ONLY in the desktop Controls editor — see [ACTIONS.md](ACTIONS.md)) toggles via the desktop default (`enabled`) + the phone's Settings → `sets` picker (per-device via the shell's `Android.prefGet/prefSet` SharedPreferences bridge — NOT bare localStorage, which is per-ORIGIN and silently split state between the LAN and Tailscale addresses, the "picker rotates" bug of 2026-08-05; localStorage stays only as dev-browser fallback); the app sets ride along in layout focus and DO charge the count (owner 2026-08-06 — free app sets let the picker promise 8 while the wheel silently dropped two): what they hold is the largest group that can appear TOGETHER, grouped by process, so Chrome+Explorer+VSCode cost one slot while VSCode+Claude cost two and leave six for the rest, the picker states it (`N of 8 used — M held for app shortcuts`) and refuses the overflowing tick; hard cap 8 — **a LAW over the STORED state, not a check that runs on a tap** (owner 2026-08-06, after finding NINE ticked by default: the picker printed `9 of 8 used` while the wheel silently dropped two). Two sources fed it and both are guarded — prefs saved before app sets started charging are normalized on arrival (`enforceWheelCap`, the phone toasts what gave way), and the shipped `actions.json` itself had seven categories enabled under a two-slot reserve (Cursor is off by default now; the guard refuses a shipped file that ticks past the cap, and the desktop Controls editor counts the same reserve). Which set gives way is his rule — the app-aware set first, then the last optional basic, and only a set in the CHARGING group frees a slot; non-required sets bumped from the end; any set may carry per-orientation button arrangement (`order_land`/`order_port`, shipped order = restorable default). **Where each set SITS on the wheel is his too** (build round R5, owner 2026-08-07): a top-level `wheel_order` array in actions.json (a list of set names), set from the desktop Controls editor's **Wheel order…** dialog — position 1 is 12 o'clock, the rest follow clockwise; a set not currently riding is skipped and the ring closes up without a hole; a set the order does not mention rides at the end. It reaches the phone only because `web._load_actions()` passes it — that whitelist returns a FIXED set of keys, and a field missing from it is a feature the desktop saves and the phone never sees. **And a field we invent must be PROVEN to reach HIS file, not ours** (owner report 2026-08-07, the fifth on one bug): the user's actions.json is seeded once at install and never replaced, `merge_shipped_pools` is its only path, and it carried a HARDCODED LIST of field names — so `"agent": "claude"` never arrived, the Claude set could only match by TITLE, and Claude Code names its VS Code tab after the CONVERSATION: unsatisfiable, forever. The process failure is the bigger half — every guard built its "user file" as `user = copy(shipped)`, so it already had the new field before the merge ran, and nobody ever opened `%LOCALAPPDATA%/VibeCoder/actions.json`. The rule is now INVERTED and forward-compatible: OURS = every key not listed as his (always taken from shipped, deleted if we retired it); HIS = `active`/`order_land`/`order_port`/`enabled` and button `label` renames, plus top-level `wheel_order`/`left`/`right`/`custom_sets` — kept if present, SEEDED if absent. A new owner-editable key MUST join `OWNER_SET_KEYS`/`OWNER_TOP_KEYS` in the same commit that makes it editable. Gate: `tests/test_actions_migration.py`, fail-closed in `build.py` — it starts from an OLDER file shape and plants a field name nobody has invented, because a gate naming today's field ships broken on tomorrow's. **Every set carries a POOL, not four fixed buttons** (owner 2026-08-05): `buttons` may hold more commands than a D-pad shows — the RESERVES (VSCode Markdown preview `ctrl+shift+v` + next/prev tab, Chrome prev tab, Explorer tab hops, Edit Save/Undo/Redo, Windows snap/min/max, …) — and `active` names the four that ride, BY ID (`id | action | chord | key | label`, never an index, so a later version inserting a pool command cannot re-point the owner's choice; no `active` = the first four, so pre-pool files keep working). The owner ticks his four in the desktop Controls editor, whose set list is split into three sections — **Standard** / **App-aware** / **Custom** — with the arrangement box captioned Arrangement over **D-pad (landscape)** and **Stack (portrait)** and a single **Default** button under them (owner 2026-08-06), and which now also edits `app_sets` and REFRESHES every built-in pool from the shipped file on open (`merge_shipped_pools` — the root cause of "Settings still shows Anywhere after an update": the user copy in %LOCALAPPDATA% is seeded once and otherwise never sees a new version's defaults). The commands of built-in and app sets are ours — the owner picks from the pool, he does not rewrite them; custom sets stay fully editable (owner decision 2026-08-05). Top-left **Layout (+)** (arms the next tap to pick a window for a new layout — the old Move/pan button is GONE, owner 2026-08-02) and top-right **Hide** (hide all controls) are fixed; top-center is the **layout bar** (big SVG ‹ › arrows OUTSIDE a framed app/tab name, plus ✕ to remove the focused layout) — it exists only while at least one layout does, cycles Desktop → layout 1 → … , and tapping the framed name opens the **layout list** (every layout at once, for when stepping one by one is too slow — owner 2026-08-03), where each row is `[icon][⭐][name][shape drawing][⚙]` (task 175, 2026-08-09 — his structural point: acts on an EXISTING layout kept multiplying per row, "sve to treba ubaciti pod neku zajedničku settings ikonicu" — lang-ok: owner quote): the ⚙ opens that layout's settings sheet (`client/layout-settings.js`, split out of layouts.js when it crossed 1,000 lines) — Rename, Aspect ratio (the old panel, one door in), Orientation and Arrangement (ONE TAP acts, via `layout_grid`, per his 2026-08-05 rule "korisnik odabere a program automatski odradi" — lang-ok: owner quote), and Take a window out — while the shape drawing stays the tappable shortcut to the member chooser. The rename pencil and the aspect chip left the row. **Every row DRAWS its own shape** (owner request 2026-08-09, task 164): a name alone made a solo window, a two-split and a four-grid read identically until he opened one, so the row carries a little diagram of the real arrangement, keyed by (member count, arrangement, orientation) from what `layout_state` already sends. The catalogue is his own sheet (`UV/grid_variations.png`, 2026-08-07 — LANDSCAPE and PORTRAIT columns, rows of 2 / 3 / 4): **6 grid shapes + solo = 7, and 14 with the orientations**, with FOUR arrangements for a three and exactly one each for a two and a four — the asymmetry is the rule, so no panel may offer a choice that does not exist. It is a DRAWING and never a word ("GRID kada korisnik bira budu skice ... a ne tekstovi tipa 'GRID 2x1'" — lang-ok: owner quote) and never a font glyph (the ✥ character came out a blunt cross on his phone, 2026-08-05). The geometry lives in `client/grid-icons.js`, PURE so its gate runs it whole — one copy on this side, which is why `client/grids.js` delegates to it — and only "2" changes its PARTITION with orientation while the other six lean their BOX, since on a fixed square a portrait three and a landscape three come out pixel-for-pixel identical. Gate: `tests/test_grid_icons.py`, fail-closed in `build.py` (0s/6); it proves every one of the 14 draws its OWN silhouette and compares each partition, number for number, against `server/grids.py` — the arithmetic that really places the windows, which `grids.js` had been asked to mirror since 2026-08-07 with nothing checking it. In layout focus the view is BOUND to the member window's region: **two fingers pinch-zoom and pan there exactly as on the desktop** (owner 2026-08-04 — no reason for zoom to be missing), with the region fitted to the screen as the maximum VISUAL zoom-out and the default on every layout switch — pinching out PAST the fitted view continues as the **font-zoom staircase** (owner 2026-08-05): each step sends Ctrl+`-` to the layout window so its own content shrinks (wide articles become fully visible), and pinching back in undoes those steps with Ctrl+`=`. **One pinch, one side** (owner 2026-08-06): the fitted view with the content at 100% is a WALL, not a point on one continuous scale — a single pinch may only travel on the side it started on (zoomed in → closing the fingers stops at the fitted view and never touches the content; content shrunk → spreading stops at exactly 100% and never becomes visual zoom), and crossing costs a finger lift, because sliding through in one motion made the most-wanted state — text at exactly 100% — nearly impossible to land on. A pinch that STARTS on the wall is undecided until the fingers travel a small deadzone: spread = zoom, close = font; layout focus ONLY, effect is whatever the focused app does with those chords (browsers/editors scale, some apps ignore it — accepted); the view and the cursor stay clamped inside the region ( phone rotation locked to the layout's orientation via the `Android.lockOrientation` bridge; full desktop = rotation free). Everything else — mouse modes, keyboard, monitor, snap, and chord shortcuts — is a category button defined in the owner-edited `actions.json` (see [ACTIONS.md](ACTIONS.md)), re-read every connection; the client renders from config and never hardcodes the layout. Custom-set editing UI and login belong to the future desktop GUI (owner decision).
- **Canvas modes are toggles; mouse BUTTONS are click/hold** (owner 2026-08-04, refining the 2026-07-26 rule): a single active `touchMode` — `move` (default: cursor only, never clicks) · `scroll` (· `drag`, unshipped — redundant) — decides what one finger ON THE CANVAS does; tapping a mode button toggles it; two fingers always pinch-zoom; nothing on the canvas is ever a tap-action. The **Click/Right/Middle buttons** however are real mouse buttons: tap = click, holding the finger ON THE BUTTON holds the PC button down (drag/select with the other hand steering) — `press {button, down}`; pointercancel always releases, so no PC button can stay stuck. Do not reintroduce tap-to-act on the canvas or a separate `hover` mode — cursor-only IS the default.
- **The soft keyboard COVERS, it never MOVES the picture** (owner decree 2026-08-07, withdrawing his own request of 2026-08-03 after living with it). The canvas keeps its FULL height — it is never SQUEEZED, that half stands — but it is no longer LIFTED by the keyboard's height: the row he types into is almost never at the very bottom of the PC screen, so a keyboard-sized lift carried the very text he was watching off the top (*"izbaci tekst koji se kuca iz vidokruga"*). `kbShift` is 0 and stays 0.
- **Keyboard capture is invisible** (owner decision 2026-07-22, reversing the visible top bar — the bar only mirrored what the PC screen already shows): the `<textarea>` stays `opacity:0`; typed/dictated text is watched in the focused box on the streamed PC screen itself. The IME's ↵ key and IME-committed `"\n"` both become **Shift+Enter** (new row — messengers keep typing instead of sending); the real Enter (activate/send) is the D-pad **Enter** button (`key_special enter`) in `actions.json`.
- **Phone → PC content** (the Attach set, owner 2026-08-04 — grew out of the single `upload` action): every source ends the same way — content in the **PC clipboard**, **Ctrl+V injected by the server** (picking was the whole gesture, owner 2026-07-22). ONE image → `/upload` → CF_DIB bitmap (pastes into any image box; Pillow-first decode with `pillow-heif` — phone cameras default to HEIC — EXIF-corrected, cv2 fallback). SEVERAL files or any non-image (PDF…) → `/upload_files` → temp drop folder + **CF_HDROP** (real files, like Copy in Explorer; the previous upload's temp files are cleared on the NEXT upload, not after paste — the target app may read the clipboard lazily). The shell's file chooser honors the page inputs: `multiple` (gallery/Files), `capture` (Camera → TakePicture via FileProvider; CAMERA runtime permission — the manifest already declares it for QR). Shot = PC screenshot of the viewed region (see the protocol above). `snap` (full monitor, clipboard only) is not in the default layout.
- **The desktop app is dark or light, and the switch is a sun/moon pill** (owner build round R3, 2026-08-07). The palette lives in `server/gui/theme.py` as TWO tables read LATE — `TOKENS` is the active one, mutated in place, and `qss()` builds the stylesheet from it — and `apply_theme` puts that stylesheet on the **QApplication**, never on a window: a per-widget stylesheet wins over its parent's, so styling the main window alone would have left Controls, Traffic, Settings and the wheel-order dialog behind. The pill (`server/gui/switch.py`, ported from PromptPainter) sits top-right of the main window's top bar after the RUNNING pill AND as a row in Settings → Appearance; its knob slides on a smoothstep over ~600 ms while the palette changes UNDER a snapshot of every open window that fades out over ~300 ms, so the repaint cascade is never seen. Both switches call one function and neither remembers a state of its own; the choice is `ui_theme` in user settings. Every window is audited and screenshotted in BOTH palettes (`tests/test_layout_audit_qt.py`), and the phone in all six theme × fill combinations with its contrast check reading the live `--surface-0` (`tests/test_layout_audit.py`).
- **Updates flow downhill: GitHub → PC → phone** (owner request 2026-07-22). Only the DESKTOP app checks the internet: once per start it asks the public repo's latest GitHub release (`updates.py`, `update_repo` in config) and shows an in-window "Update to vX" button — download installer, launch, quit. The PHONE never checks the internet: `config.app_version` tells the page what the PC runs, the `Android.appVersion()` bridge tells it what the shell is, and a newer PC shows the "Update the app" banner → `Android.update()` downloads `/app.apk` from the SAME PC via the system browser. Publishing a GIT RELEASE (tag + `VibeCoder_Setup.exe` asset) is what lights the desktop path up.
- **Rendering is two layers** (owner request — smooth pan/zoom): a full-monitor **base** frame kept in memory (drawn always, so motion never flashes blank) plus the sharp **region** crop drawn on top when zoomed. Full native resolution is never streamed live (bandwidth); the base is the last full frame, the region refreshes while zoomed.
- Region streaming, JPEG: when zoomed, the client requests its visible region (`viewport`); the server crops the 4K frame to it before downscaling — zoom stays sharp at constant bandwidth. Full frame = region (0, 0, 1, 1).
- **Region streaming, H.264** (owner order 2026-08-12 — the phone never decodes pixels it does not show): the per-client ffmpeg CROPS to the focused layout's region (even-aligned for yuv420p; `H264Session._crop_rect`), so a quarter-width layout costs a quarter-width decode and every bit of bitrate lands on what is watched. `config` carries **`stream_region`** — the monitor-normalized rect the video really covers (the even-rounded crop's own normalization, never the request's decimals) — and the page maps the video onto that rect (`render.js`) instead of the full monitor; absent = full frame, an old server keeps working. A region change (focus, switch, aspect, desktop) ends the mismatched session at the ONE choke point every layout change passes (`layout_api.send_layout_state` compares the live `conn["region"]` against `conn["stream_region"]`, the intention copy web.py records at open — two reads of one source, so equality is exact) and the loop reopens with the fresh crop + a fresh `config`; the phone's decode ceiling (`client/decode-caps.js`) sizes itself by the CROP (`effectiveWidth` × region), and the client restates `quality` when a region change moves that cap (`restateQualityIfChanged` — the server re-applies stored quality to every new session, so a cap earned by the full desktop would otherwise hold an easy layout crop at the capped fps). Gate: `tests/test_region_stream.py`, fail-closed in `build.py` (0ap/6), driven with his own live layout's region from his own log.

## Testing Notes

- **The Qt layout audit runs FIRST of the Qt guards** (`tests/run_guards.py`, 2026-08-07). `test_controls_sets` sets `QT_QPA_PLATFORM=offscreen` at import and builds a QApplication with it, so every Qt guard after it inherited a platform with NO SYSTEM FONTS — the audit still measured, but every screenshot it wrote came out as rows of tofu boxes, and those screenshots are the DESIGN REVIEW's entire evidence. It is also the source of the long-standing "windows measure twice as wide inside run_guards" note. Ordering is the whole fix; never move it back, and never grade a picture you did not cause to be written.
- Gesture disambiguation (tap vs drag vs scroll) can only be tuned on a **real touch device** — do not assume defaults are right.
- Test Unicode injection against real targets (VSCode, browser inputs) — VK_PACKET has known quirks in some apps (e.g. Windows Terminal surrogate-pair bugs).
- After long-running capture sessions, verify frame latency fresh — restart before measuring (see [Code Rules](../../rules/CODE.md) → Profiling).
