# Owner's inbox — free-form specs/images, never tracked (root CLAUDE.md — Universal Conduct, UV/ folder)
UV/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Logs
logs/

# Regenerated on every server start (contains the live pairing token)
PAIRING_QR.png

# Downloaded tunnel binary
bin/

# Build output
dist/
build/
*.spec
setup/cert/
setup/icon*.ico
setup/vendor/
version_info.txt

# Android build artifacts (the APK lands in dist/)
android/.gradle/
android/local.properties
android/keystore/
android/app/build/

# Claude Code working folder - never tracked (owner decree 2026-08-09), with
# ONE exception: the window registry `uv shot` reads. It is not session
# residue - it is the list of this project's windows, and a fresh clone that
# lacks it cannot photograph a single one.
.claude/*
!.claude/uv_windows.py
.claude/sessions/
.claude/evidence/
